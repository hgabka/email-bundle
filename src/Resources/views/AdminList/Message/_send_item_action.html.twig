{% if isNew or entity.isPrepareable() %}
    <button name="save_and_send" type="submit" class="btn btn-secondary btn--raise-on-hover">
        {{ itemAction.getLabelFor(isNew ? null : entity) | trans }}
    </button>
{% endif %}